<table align="center" class="table table-striped table-hover">
  <tr>
    <th>State</th>
    <th>Price</th>

  </tr>
  <% @prices.each do |p| %>
    <tr>
      <td><%= p.state %></td>
      <td>$<%= p.price %> per galon</td>
    </tr>
  <% end %>
</table>